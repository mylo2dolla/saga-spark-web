# Mythic API (VPS) configuration
#
# Copy to .env on the VPS (same directory as docker-compose.yml) and fill values.
# Do NOT commit real secrets.

# Public domain that will serve /functions/v1/*
MYTHIC_API_DOMAIN=api.example.com

# Optional: used by Caddy for Let's Encrypt registration
CADDY_EMAIL=

# Required Supabase configuration
SUPABASE_URL=
SUPABASE_PROJECT_REF=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: comma-separated list of allowed frontend origins. Empty = allow all.
MYTHIC_ALLOWED_ORIGINS=

# OpenAI is required for mythic-dungeon-master and mythic-tts
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com
OPENAI_MODEL=gpt-4o-mini
OPENAI_TTS_MODEL=tts-1

# Optional: legacy generator endpoints
LLM_PROVIDER=
LLM_MODEL=
GROQ_API_KEY=
GROQ_BASE_URL=https://api.groq.com/openai
GROQ_MODEL=llama-3.3-70b-versatile

# Logging + global rate limiting
LOG_LEVEL=info
GLOBAL_RATE_LIMIT_MAX=240
GLOBAL_RATE_LIMIT_WINDOW_MS=60000

